apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: praxis-test
  namespace: praxis-test
spec:
  gatewayClassName: istio
  listeners:
    - name: http
      port: 8080
      protocol: HTTP
